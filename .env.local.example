
# Dedicated CAD worker and shared artifact volume. Deploy the worker in an isolated
# Node container with this path mounted on the application service.
GEMINI_API_KEY="replace-with-your-server-side-key"

