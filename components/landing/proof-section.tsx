import { CadModelVisual } from '@/components/cad/cad-model-visual';
import type { CadModelResult } from '@/components/cad/replicad-models';

export function ProofSection({ model }: { model: CadModelResult }) {
  return <section className="reference-section proof" id="proof"><CadModelVisual model="spur-gear" initialResult={model} className="proof-model" eyebrow="MODEL INSPECTION / 01" /><div className="reference-copy"><p className="section-kicker">ENGINEERING PROOF</p><h2>The output is a system.</h2><p>Native solids, measurable results, and a design history your team can understand after the first generation.</p><div className="proof-metrics"><div className="proof-metric"><strong>{model.metrics.volume.toLocaleString()}</strong><span>mm³ solid volume</span></div><div className="proof-metric"><strong>{model.metrics.surfaceArea.toLocaleString()}</strong><span>mm² surface area</span></div></div><div className="proof-list"><div><strong>Native geometry</strong><span>Built through Replicad and OpenCascade, not a flat visual approximation.</span></div><div><strong>Inspectable decisions</strong><span>Parameters stay close to the model so the next change has context.</span></div><div><strong>Ready to iterate</strong><span>Take the first solid into a project and keep building from the same intent.</span></div></div></div></section>;
}
