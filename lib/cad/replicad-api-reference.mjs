// AUTO-GENERATED by scripts/gen-replicad-api.mjs from node_modules/replicad — do not edit by hand.
// Re-run `npm run gen:replicad-api` after upgrading replicad.
export const REPLICAD_API_REFERENCE = "// ---- CLASSES (methods of the objects you get back) ----\nabstract class _1DShape<Type extends TopoDS_Shape> extends Shape<Type> {\n  get repr(): string;\n  get curve(): Curve;\n  get startPoint(): Vector;\n  get endPoint(): Vector;\n  tangentAt(position?: number): Vector;\n  pointAt(position?: number): Vector;\n  get isClosed(): boolean;\n  get isPeriodic(): boolean;\n  get period(): number;\n  get geomType(): CurveType;\n  get length(): number;\n  get orientation(): \"forward\" | \"backward\";\n  flipOrientation(): Type;\n}\n\nclass _3DShape<Type extends TopoDS_Shape> extends Shape<Type> implements Shape3DLike<Shape3D, ShapeMesh, AnyShape, {\n  tolerance?: number;\n  angularTolerance?: number;\n  }> {\n  fuse(other: Shape3D, options?: BooleanOperationOptions): Shape3D; // Builds a new shape out of the two, fused, shapes\n  cut(tool: Shape3D, options?: BooleanOperationOptions): Shape3D; // Builds a new shape by removing the tool tape from this shape\n  intersect(tool: AnyShape): Shape3D; // Builds a new shape by intersecting this shape and another\n  cutPlane(plane?: Plane | PlaneName, offset?: number, keep?: PlaneSide): Solid | Compound | null; // Cuts this shape with a plane and retains one of its half-spaces.\n  meshShape(options?: { tolerance?: number; angularTolerance?: number; }): MeshShape;\n  shell(config: { filter: FaceFinder; thickness: number; }, tolerance?: number): Shape3D; // Hollows out the current shape, removing the faces found by the `filter` and\n  shell(thickness: number, finderFcn: FinderFunction<FaceFinder, AnyShape>, tolerance?: number): Shape3D;\n  fillet(radiusConfig: RadiusConfig<FilletRadius>, filter?: FinderFunction<EdgeFinder, AnyShape>): Shape3D; // Creates a new shapes with some edges filletted, as specified in the\n  chamfer(radiusConfig: RadiusConfig<ChamferRadius>, filter?: FinderFunction<EdgeFinder, AnyShape>): Shape3D; // Creates a new shapes with some edges chamfered, as specified in the\n  draft(angle: number, faceFinder: FinderFunction<FaceFinder, AnyShape>, neutralPlane?: Plane | PlaneName): Shape3D; // Applies a draft angle to selected faces of the shape.\n}\n\nclass BaseSketcher2d {\n  constructor(origin?: Point2D);\n  get penPosition(): Point2D; // Returns the current pen position as [x, y] coordinates\n  get penAngle(): number; // Returns the current pen angle in degrees\n  movePointerTo(point: Point2D): this;\n  lineTo(point: Point2D): this;\n  line(xDist: number, yDist: number): this;\n  vLine(distance: number): this;\n  hLine(distance: number): this;\n  vLineTo(yPos: number): this;\n  hLineTo(xPos: number): this;\n  polarLineTo([r, theta]: Point2D): this;\n  polarLine(distance: number, angle: number): this;\n  tangentLine(distance: number): this;\n  threePointsArcTo(end: Point2D, midPoint: Point2D): this;\n  threePointsArc(xDist: number, yDist: number, viaXDist: number, viaYDist: number): this;\n  sagittaArcTo(end: Point2D, sagitta: number): this;\n  sagittaArc(xDist: number, yDist: number, sagitta: number): this;\n  vSagittaArc(distance: number, sagitta: number): this;\n  hSagittaArc(distance: number, sagitta: number): this;\n  bulgeArcTo(end: Point2D, bulge: number): this;\n  bulgeArc(xDist: number, yDist: number, bulge: number): this;\n  vBulgeArc(distance: number, bulge: number): this;\n  hBulgeArc(distance: number, bulge: number): this;\n  tangentArcTo(end: Point2D): this;\n  tangentArc(xDist: number, yDist: number): this;\n  ellipseTo(end: Point2D, horizontalRadius: number, verticalRadius: number, rotation?: number, longAxis?: boolean, sweep?: boolean): this;\n  ellipse(xDist: number, yDist: number, horizontalRadius: number, verticalRadius: number, rotation?: number, longAxis?: boolean, sweep?: boolean): this;\n  halfEllipseTo(end: Point2D, minorRadius: number, sweep?: boolean): this;\n  halfEllipse(xDist: number, yDist: number, minorRadius: number, sweep?: boolean): this;\n  bezierCurveTo(end: Point2D, controlPoints: Point2D | Point2D[]): this;\n  quadraticBezierCurveTo(end: Point2D, controlPoint: Point2D): this;\n  cubicBezierCurveTo(end: Point2D, startControlPoint: Point2D, endControlPoint: Point2D): this;\n  smoothSplineTo(end: Point2D, config?: SplineConfig): this;\n  smoothSpline(xDist: number, yDist: number, splineConfig?: SplineConfig): this;\n  customCorner(radius: number | ((first: Curve2D, second: Curve2D) => Curve2D[]), mode?: \"fillet\" | \"chamfer\" | \"dogbone\"): this; // Changes the corner between the previous and next segments.\n}\n\nclass BoundingBox extends WrappingObj<Bnd_Box> {\n  constructor(wrapped?: Bnd_Box);\n  static fromBounds(min: Point, max: Point): BoundingBox;\n  get repr(): string;\n  get bounds(): [SimplePoint, SimplePoint];\n  get center(): SimplePoint;\n  get width(): number;\n  get height(): number;\n  get depth(): number;\n  add(other: BoundingBox): void;\n  isOut(other: BoundingBox): boolean;\n}\n\nclass Compound extends _3DShape<TopoDS_Compound> {\n\n}\n\nclass CompoundSketch implements SketchInterface {\n  sketches: Sketch[];\n  constructor(sketches: Sketch[]);\n  delete(): void;\n  get outerSketch(): Sketch;\n  get innerSketches(): Sketch[];\n  get wires(): AnyShape;\n  face(): Face;\n  extrude(extrusionDistance: number, { extrusionDirection, extrusionProfile, twistAngle, origin, }?: { extrusionDirection?: Point; extrusionProfile?: ExtrusionProfile; twistAngle?: number; origin?: Point; }): Shape3D;\n  revolve(revolutionAxis?: Point, { origin, angle }?: { origin?: Point; angle?: number; }): Shape3D; // Revolves the drawing on an axis (defined by its direction and an origin\n  loftWith(otherCompound: this, loftConfig: LoftConfig): Shape3D;\n}\n\nclass CompSolid extends _3DShape<TopoDS_CompSolid> {\n\n}\n\nclass CornerFinder extends Finder<Corner, Blueprint> {\n  clone(): CornerFinder;\n  inList(elementList: Point2D[]): this; // Filter to find corner that have their point are in the list.\n  atDistance(distance: number, point?: Point2D): this; // Filter to find elements that are at a specified distance from a point.\n  atPoint(point: Point2D): this; // Filter to find elements that contain a certain point\n  inBox(corner1: Point2D, corner2: Point2D): this; // Filter to find elements that are within a box\n  ofAngle(angle: number): this; // Filter to find corner that a certain angle between them - only between\n  shouldKeep(element: Corner): boolean;\n}\n\nclass Drawing implements DrawingInterface {\n  constructor(innerShape?: Shape2D);\n  clone(): Drawing;\n  serialize(): string;\n  get boundingBox(): BoundingBox2d;\n  stretch(ratio: number, direction: Point2D, origin: Point2D): Drawing;\n  get repr(): string;\n  rotate(angle: number, center?: Point2D): Drawing;\n  translate(xDist: number, yDist: number): Drawing;\n  translate(translationVector: Point2D): Drawing;\n  scale(scaleFactor: number, center?: Point2D): Drawing;\n  mirror(centerOrDirection: Point2D, origin?: Point2D, mode?: \"center\" | \"plane\"): Drawing;\n  cut(other: Drawing): Drawing; // Builds a new drawing by cuting another drawing into this one\n  fuse(other: Drawing): Drawing; // Builds a new drawing by merging another drawing into this one\n  intersect(other: Drawing): Drawing; // Builds a new drawing by intersection this drawing with another\n  fillet(radius: number, filter?: FinderFunction<CornerFinder, Shape2D>): Drawing; // Creates a new drawing with some corners filletted, as specified by the\n  chamfer(radius: number, filter?: FinderFunction<CornerFinder, Shape2D>): Drawing; // Creates a new drawing with some corners filletted, as specified by the\n  sketchOnPlane(inputPlane: Plane): SketchInterface | Sketches;\n  sketchOnPlane(inputPlane?: PlaneName, origin?: Point | number): SketchInterface | Sketches;\n  sketchOnFace(face: Face, scaleMode: ScaleMode): SketchInterface | Sketches;\n  punchHole(shape: AnyShape, faceFinder: SingleFace, options?: { height?: number; origin?: Point; draftAngle?: number; }): AnyShape;\n  toSVG(margin?: number): string;\n  toSVGViewBox(margin?: number): string;\n  toSVGPaths(): string[] | string[][];\n  offset(distance: number, offsetConfig?: Offset2DConfig): Drawing;\n  approximate(target: \"svg\" | \"arcs\", options?: ApproximationOptions): Drawing;\n  get blueprint(): Blueprint;\n}\n\nclass DrawingPen extends BaseSketcher2d implements GenericSketcher<Drawing> {\n  constructor(origin?: Point2D);\n  done(): Drawing;\n  close(): Drawing;\n  closeWithMirror(): Drawing;\n  closeWithCustomCorner(radius: number, mode?: \"fillet\" | \"chamfer\"): Drawing; // Stop drawing, make sure the sketch is closed (by adding a straight line to\n}\n\nclass Edge extends _1DShape<TopoDS_Edge> {\n\n}\n\nclass EdgeFinder extends Finder3d<Edge> {\n  clone(): EdgeFinder;\n  inDirection(direction: Direction): this; // Filter to find edges that are in a certain direction\n  ofLength(length: number | ((l: number) => boolean)): this; // Filter to find edges of a certain length\n  ofCurveType(curveType: CurveType): this; // Filter to find edges that are of a cetain curve type.\n  parallelTo(plane: Plane | StandardPlane | Face): this; // Filter to find edges that are parallel to a plane.\n  inPlane(inputPlane: PlaneName | Plane, origin?: Point | number): this; // Filter to find edges that within a plane.\n  shouldKeep(element: Edge): boolean;\n}\n\nclass Face extends Shape<TopoDS_Face> {\n  get surface(): Surface;\n  get orientation(): \"forward\" | \"backward\";\n  flipOrientation(): Face;\n  get geomType(): SurfaceType;\n  get UVBounds(): FaceUVBounds;\n  pointOnSurface(u: number, v: number): Vector;\n  uvCoordinates(point: Point): [number, number];\n  normalAt(locationVector?: Point): Vector;\n  get center(): Vector;\n  outerWire(): Wire;\n  innerWires(): Wire[];\n  triangulation(index0?: number): FaceTriangulation | null;\n}\n\nclass FaceFinder extends Finder3d<Face> {\n  clone(): FaceFinder;\n  parallelTo(plane: Plane | StandardPlane | Face): this; // Filter to find faces that are parallel to plane or another face\n  ofSurfaceType(surfaceType: SurfaceType): this; // Filter to find faces that are of a cetain surface type.\n  inPlane(inputPlane: PlaneName | Plane, origin?: Point | number): this; // Filter to find faces that are contained in a plane.\n  shouldKeep(element: Face): boolean;\n}\n\nabstract class Finder<Type, FilterType> {\n  abstract shouldKeep(t: Type): boolean; // Check if a particular element should be filtered or not according to the\n  constructor();\n  delete(): void;\n  and(findersList: ((f: this) => this)[]): this; // Combine logically a set of filter with an AND operation.\n  not(finderFun: (f: this) => this): this; // Invert the result of a particular finder\n  either(findersList: ((f: this) => this)[]): this; // Combine logically a set of filter with an OR operation.\n  find(shape: FilterType, options: { unique: true; }): Type; // Returns all the elements that fit the set of filters defined on this\n  find(shape: FilterType): Type[];\n  find(shape: FilterType, options: { unique?: false; }): Type[];\n}\n\nabstract class Finder3d<Type extends FaceOrEdge> extends Finder<Type, AnyShape> {\n  when(filter: (filter: FilterFcn<Type>) => boolean): this; // Filter to find elements following a custom function.\n  inList(elementList: Type[]): this; // Filter to find elements that are in the list.\n  atAngleWith(direction?: Direction, angle?: number): this; // Filter to find elements that are at a specified angle (in degrees) with\n  atDistance(distance: number, point?: Point, tolerance?: number): this; // Filter to find elements that are at a specified distance from a point,\n  containsPoint(point: Point): this; // Filter to find elements that contain a certain point\n  near(point: Point, tolerance?: number): this; // Filter to find elements that are near a certain point, within the given\n  withinDistance(distance: number, point?: Point): this; // Filter to find elements that are within a certain distance from a point.\n  inBox(corner1: Point, corner2: Point): this; // Filter to find elements that are within a box\n  inShape(shape: AnyShape): this; // Filter to find elements that are within a generic shape\n}\n\nclass PhysicalProperties extends WrappingObj<GProp_GProps> {\n  get centerOfMass(): [number, number, number];\n}\n\nclass Plane {\n  oc: OpenCascadeInstance;\n  xDir: Vector;\n  yDir: Vector;\n  zDir: Vector;\n  constructor(origin: Point, xDirection?: Direction | null, normal?: Direction);\n  delete(): void;\n  clone(): Plane;\n  get origin(): Vector;\n  set origin(newOrigin: Vector);\n  translateTo(point: Point): Plane;\n  translate(xDist: number, yDist: number, zDist: number): Plane;\n  translate(vector: Point): Plane;\n  translateX(xDist: number): Plane;\n  translateY(yDist: number): Plane;\n  translateZ(zDist: number): Plane;\n  pivot(angle: number, direction?: Direction): Plane;\n  rotate2DAxes(angle: number): Plane;\n  _calcTransforms(): void;\n  setOrigin2d(x: number, y: number): void;\n  toLocalCoords(vec: Vector): Vector;\n  toWorldCoords(v: Point): Vector;\n}\n\nclass Shape<Type extends TopoDS_Shape> extends WrappingObj<Type> {\n  constructor(ocShape: Type);\n  clone(): this;\n  serialize(): string;\n  get hashCode(): number;\n  get isNull(): boolean;\n  isSame(other: AnyShape): boolean;\n  isEqual(other: AnyShape): boolean;\n  split(plane?: Plane | PlaneName, offset?: number, tolerance?: number): PlaneSplitResult<Solid | Compound>; // Splits the solid parts of this shape with an oriented plane and groups\n  asShape3D(): Shape3D; // Asserts that this shape is a 3D shape (Shell, Solid, CompSolid, or\n  simplify(): this; // Simplifies the shape by removing unnecessary edges and faces\n  translate(xDist: number, yDist: number, zDist: number): this; // Translates the shape of an arbitrary vector\n  translate(vector: Point): this;\n  translateX(distance: number): this; // Translates the shape on the X axis\n  translateY(distance: number): this; // Translates the shape on the Y axis\n  translateZ(distance: number): this; // Translates the shape on the Z axis\n  rotate(angle: number, position?: Point, direction?: Direction): this; // Rotates the shape\n  mirror(inputPlane?: Plane | PlaneName | Point, origin?: Point): this; // Mirrors the shape through a plane\n  scale(scale: number, center?: Point): this; // Returns a scaled version of the shape\n  get edges(): Edge[];\n  get faces(): Face[];\n  get solids(): Solid[];\n  get wires(): Wire[];\n  get boundingBox(): BoundingBox;\n  mesh(options?: MeshOptions): ShapeMesh; // Exports the current shape as a set of triangle. These can be used by threejs\n  meshEdges(options?: MeshOptions): ShapeEdgeMesh; // Exports the current shape as a set of lines. These can be used by threejs\n  blobSTEP(): Blob; // Exports the current shape as a STEP file as a Blob\n  blobSTL(options?: STLExportOptions): Blob; // Exports the current shape as a STL file as a Blob\n}\n\nclass Shell extends _3DShape<TopoDS_Shell> {\n\n}\n\nclass Sketch implements SketchInterface {\n  wire: Wire;\n  _defaultOrigin: Vector;\n  _defaultDirection: Vector;\n  constructor(wire: Wire, { defaultOrigin, defaultDirection, }?: { defaultOrigin?: Point; defaultDirection?: Point; });\n  get baseFace(): Face | null | undefined;\n  set baseFace(newFace: Face | null | undefined);\n  delete(): void;\n  clone(): Sketch;\n  get defaultOrigin(): Vector;\n  set defaultOrigin(newOrigin: Point);\n  get defaultDirection(): Vector;\n  set defaultDirection(newDirection: Point);\n  face(): Face; // Transforms the lines into a face. The lines should be closed.\n  wires(): Wire;\n  faces(): Face;\n  revolve(revolutionAxis?: Point, { origin, angle }?: { origin?: Point; angle?: number; }): Shape3D; // Revolves the drawing on an axis (defined by its direction and an origin\n  extrude(extrusionDistance: number, { extrusionDirection, extrusionProfile, twistAngle, origin, }?: { extrusionDirection?: Point; extrusionProfile?: ExtrusionProfile; twistAngle?: number; origin?: Point; }): Shape3D; // Extrudes the sketch to a certain distance.(along the default direction\n  sweepSketch(sketchOnPlane: (plane: Plane, origin: Point) => this, sweepConfig?: GenericSweepConfig): Shape3D; // Sweep along this sketch another sketch defined in the function\n  loftWith(otherSketches: this | this[], loftConfig?: LoftConfig, returnShell?: boolean): Shape3D; // Loft between this sketch and another sketch (or an array of them)\n}\n\nclass Sketches {\n  sketches: Array<Sketch | CompoundSketch>;\n  constructor(sketches: Array<Sketch | CompoundSketch>);\n  wires(): AnyShape;\n  faces(): AnyShape;\n  extrude(extrusionDistance: number, extrusionConfig?: { extrusionDirection?: Point; extrusionProfile?: ExtrusionProfile; twistAngle?: number; origin?: Point; }): AnyShape; // Extrudes the sketch to a certain distance.(along the default direction\n  revolve(revolutionAxis?: Point, config?: { origin?: Point; angle?: number; }): AnyShape; // Revolves the drawing on an axis (defined by its direction and an origin\n}\n\nclass Solid extends _3DShape<TopoDS_Solid> {\n\n}\n\nclass Vertex extends Shape<TopoDS_Vertex> {\n  asTuple(): [number, number, number];\n}\n\nclass Wire extends _1DShape<TopoDS_Wire> {\n  offset2D(offset: number, kind?: \"arc\" | \"intersection\" | \"tangent\"): Wire;\n}\n\n// ---- FUNCTIONS / CONSTANTS (destructure from the `replicad` argument) ----\nconst addHolesInFace: (face: Face, holes: Wire[]) => Face;\nconst assembleWire: (listOfEdges: (Edge | Wire)[]) => Wire;\nconst backMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's minimum Y bound.\nconst basicFaceExtrusion: (face: Face, extrusionVec: Vector) => Solid;\nconst bottomMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's minimum Z bound.\nfunction complexExtrude(wire: Wire, center: Point, normal: Point, profileShape: ExtrusionProfile | undefined, shellMode: true): [Shape3D, Wire, Wire];\nfunction complexExtrude(wire: Wire, center: Point, normal: Point, profileShape?: ExtrusionProfile, shellMode?: false): Shape3D;\nconst compoundShapes: (shapeArray: AnyShape[]) => AnyShape;\nconst createNamedPlane: (plane: PlaneName, sourceOrigin?: Point | number) => Plane;\nconst cut2D: (first: Shape2D, second: Shape2D) => Blueprint | Blueprints | CompoundBlueprint | null;\nconst DEG2RAD: number;\nfunction draw(initialPoint?: Point2D): DrawingPen; // Creates a drawing pen to programatically draw in 2D.\nfunction drawCircle(radius: number): Drawing; // Creates the `Drawing` of a circle.\nfunction drawEllipse(majorRadius: number, minorRadius: number): Drawing; // Creates the `Drawing` of an ellipse.\nfunction drawFaceOutline(face: Face): Drawing; // Creates the `Drawing` out of a face\nconst drawParametricFunction: (func: (t: number) => Point2D, { pointsCount, start, stop, closeShape }?: { pointsCount?: number | undefined; start?: number | undefined; stop?: number | undefined; closeShape?: boolean | undefined; }, approximationConfig?: BSplineApproximationConfig) => Drawing; // Creates the `Drawing` of parametric function\nconst drawPointsInterpolation: (points: Point2D[], approximationConfig?: BSplineApproximationConfig, options?: { closeShape?: boolean; }) => Drawing; // Creates the `Drawing` by interpolating points as a curve\nfunction drawPolysides(radius: number, sidesCount: number, sagitta?: number): Drawing; // Creates the `Drawing` of an polygon in a defined plane\nconst drawRectangle: typeof drawRoundedRectangle;\nfunction drawRoundedRectangle(width: number, height: number, r?: number | { rx?: number; ry?: number; }): Drawing; // Creates the `Drawing` of a rectangle with (optional) rounded corners.\nfunction drawSingleCircle(radius: number): Drawing; // Creates the `Drawing` of a circle as one single curve.\nfunction drawSingleEllipse(majorRadius: number, minorRadius: number): Drawing; // Creates the `Drawing` of an ellipse as one single curve.\nconst frontMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's maximum Y bound.\nconst fuse2D: (first: Shape2D, second: Shape2D) => Blueprint | Blueprints | CompoundBlueprint | null;\nfunction genericSweep(wire: Wire, spine: Wire, sweepConfig: GenericSweepConfig, shellMode: true): [Shape3D, Wire, Wire];\nfunction genericSweep(wire: Wire, spine: Wire, sweepConfig: GenericSweepConfig, shellMode?: false): Shape3D;\nfunction getSingleFace(f: SingleFace, shape: AnyShape): Face;\nfunction intersect2D(first: Shape2D, second: Shape2D): Blueprint | Blueprints | CompoundBlueprint | null;\nconst leftMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's minimum X bound.\nconst loft: (wires: Wire[], { ruled, startPoint, endPoint }?: LoftConfig, returnShell?: boolean) => Shape3D;\nconst makeBaseBox: (xLength: number, yLength: number, zLength: number) => Shape3D; // Builds a rectangular box of the given lengths.\nconst makeBezierCurve: (points: Point[]) => Edge;\nconst makeBox: (corner1: Point, corner2: Point) => Solid; // Creates a box with the given corner points.\nconst makeBSplineApproximation: (points: Point[], { tolerance, smoothing, degMax, degMin, }?: BSplineApproximationConfig) => Edge;\nconst makeCircle: (radius: number, center?: Point, normal?: Direction) => Edge;\nconst makeCompound: (shapeArray: AnyShape[]) => AnyShape;\nconst makeCylinder: (radius: number, height: number, location?: Point, direction?: Direction) => Solid; // Creates a cylinder with the given radius and height.\nconst makeEllipse: (majorRadius: number, minorRadius: number, center?: Point, normal?: Direction, xDir?: Direction) => Edge;\nconst makeEllipseArc: (majorRadius: number, minorRadius: number, startAngle: number, endAngle: number, center?: Point, normal?: Direction, xDir?: Direction) => Edge;\nconst makeEllipsoid: (aLength: number, bLength: number, cLength: number) => Solid; // Creates an ellipsoid with the given lengths of the axes, centred on the origin.\nconst makeFace: (wire: Wire, holes?: Wire[]) => Face;\nconst makeHelix: (pitch: number, height: number, radius: number, center?: Point, dir?: Direction, lefthand?: boolean) => Wire;\nconst makeLine: (v1: Point, v2: Point) => Edge;\nconst makeNewFaceWithinFace: (originFace: Face, wire: Wire) => Face;\nconst makeNonPlanarFace: (wire: Wire) => Face;\nconst makeOffset: (face: Face, offset: number, tolerance?: number) => Shape3D;\nfunction makePlane(plane: Plane): Plane;\nfunction makePlane(plane: PlaneName): Plane;\nfunction makePlane(plane: Plane | PlaneName): Plane;\nfunction makePlane(plane?: PlaneName, origin?: Point | number): Plane;\nconst makePlaneFromFace: (face: PlaneFace, originOnSurface?: Point2D) => Plane;\nconst makePolygon: (points: Point[]) => Face;\nfunction makeSolid(facesOrShells: Array<Face | Shell>): Solid; // Welds faces and shells into a single shell and then makes a solid.\nconst makeSphere: (radius: number) => Solid; // Creates a sphere with the given radius, centred on the origin.\nconst makeTangentArc: (startPoint: Point, startTgt: Point, endPoint: Point) => Edge;\nconst makeThreePointArc: (v1: Point, v2: Point, v3: Point) => Edge;\nconst makeVertex: (point: Point) => Vertex;\nfunction measureArea(shape: Face | Shape3D): number; // Measure the area of a shape\nfunction measureDistanceBetween(shape1: AnyShape, shape2: AnyShape): number; // Measure the distance between two shapes\nfunction measureLength(shape: AnyShape): number; // Measure the length of a shape\nfunction measureShapeLinearProperties(shape: AnyShape): LinearPhysicalProperties;\nfunction measureShapeSurfaceProperties(shape: Face | Shape3D): SurfacePhysicalProperties;\nfunction measureShapeVolumeProperties(shape: Shape3D): VolumePhysicalProperties;\nfunction measureVolume(shape: Shape3D): number; // Measure the volume of a shape\nfunction mirror(shape: TopoDS_Shape, inputPlane?: Plane | PlaneName | Point, origin?: Point): TopoDS_Shape;\nconst polysideInnerRadius: (outerRadius: number, sidesCount: number, sagitta?: number) => number; // Helper function to compute the inner radius of a polyside (even if a sagitta\nconst RAD2DEG: number;\nconst revolution: (face: Face, center?: Point, direction?: Direction, angle?: number) => Shape3D;\nconst rightMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's maximum X bound.\nfunction rotate(shape: TopoDS_Shape, angle: number, position?: Point, direction?: Direction): TopoDS_Shape;\nfunction scale(shape: TopoDS_Shape, center: Point, scale: number): TopoDS_Shape;\nconst sketchCircle: (radius: number, planeConfig?: PlaneConfig) => Sketch; // Creates the `Sketch` of a circle in a defined plane\nconst sketchEllipse: (xRadius?: number, yRadius?: number, planeConfig?: PlaneConfig) => Sketch; // Creates the `Sketch` of an ellispe in a defined plane\nconst sketchFaceOffset: (face: Face, offset: number) => Sketch; // Creates the `Sketch` of an offset of a certain face. A negative offset will\nconst sketchHelix: (pitch: number, height: number, radius: number, center?: Point, dir?: Direction, lefthand?: boolean) => Sketch; // Creates the `Sketch` of a helix\nconst sketchParametricFunction: (func: (t: number) => Point2D, planeConfig?: PlaneConfig, { pointsCount, start, stop }?: { pointsCount?: number | undefined; start?: number | undefined; stop?: number | undefined; }, approximationConfig?: BSplineApproximationConfig) => Sketch; // Creates the `Sketch` of parametric function in a specified plane\nconst sketchPolysides: (radius: number, sidesCount: number, sagitta?: number, planeConfig?: PlaneConfig) => Sketch; // Creates the `Sketch` of an polygon in a defined plane\nconst sketchRectangle: (xLength: number, yLength: number, planeConfig?: PlaneConfig) => Sketch; // Creates the `Sketch` of a rectangle in a defined plane\nconst sketchRoundedRectangle: (width: number, height: number, r?: number | { rx?: number; ry?: number; }, planeConfig?: PlaneConfig) => Sketch; // Creates the `Sketch` of a rounded rectangle in a defined plane\nconst topMost: (shape: AnyShape, tolerance?: number) => ShapeExtremumFilter; // Creates a predicate selecting elements touching the shape's maximum Z bound.\nconst TOPOLOGY_KINDS: readonly [\"vertex\", \"edge\", \"wire\", \"face\", \"shell\", \"solid\", \"solidCompound\", \"compound\"];\nfunction translate(shape: TopoDS_Shape, vector: Point): TopoDS_Shape;\nfunction twistExtrude(wire: Wire, angleDegrees: number, center: Point, normal: Point, profileShape?: ExtrusionProfile, shellMode?: false): Shape3D;\nfunction twistExtrude(wire: Wire, angleDegrees: number, center: Point, normal: Point, profileShape: ExtrusionProfile | undefined, shellMode: true): [Shape3D, Wire, Wire];\nfunction weldShellsAndFaces(facesOrShells: Array<Face | Shell>, ignoreType?: boolean): Shell; // Welds faces and shells into a single shell.\n\n// ---- TYPES / OPTION OBJECTS ----\ntype AnyShape = Vertex | Edge | Wire | Face | Shell | Solid | CompSolid | Compound;\ninterface ApproximationOptions { tolerance?: number; continuity?: \"C0\" | \"C1\" | \"C2\" | \"C3\"; maxSegments?: number; }\ntype AxisName = (typeof AXIS_NAMES)[number];\ninterface BooleanOperationOptions { optimisation?: \"none\" | \"commonFace\" | \"sameFace\"; }\ninterface BSplineApproximationConfig { tolerance?: number; degMax?: number; degMin?: number; smoothing?: null | [number, number, number]; }\ntype CartesianAxis = \"X\" | \"Y\" | \"Z\";\ntype ChamferRadius = number | { distances: [number, number]; selectedFace: FinderFunction<FaceFinder, AnyShape>; } | { distance: number; angle: number; selectedFace: FinderFunction<FaceFinder, AnyShape>; };\ntype CoordSystem = \"reference\" | { origin: Point; zDir: Direction; xDir: Direction; };\ntype Corner = { firstCurve: Curve2D; secondCurve: Curve2D; point: Point2D; };\ntype CubeFace = \"front\" | \"back\" | \"top\" | \"bottom\" | \"left\" | \"right\";\ninterface CurveLike { delete(): void; Value(v: number): gp_Pnt; IsPeriodic(): boolean; Period(): number; IsClosed(): boolean; FirstParameter(): number; LastParameter(): number; GetType?(): any; D1(v: number, p: gp_Pnt, vPrime: gp_Vec): void; }\ntype Direction = Point | AxisName;\ninterface DrawingInterface { clone(): DrawingInterface; boundingBox: BoundingBox2d; stretch(ratio: number, direction: Point2D, origin: Point2D): DrawingInterface; rotate(angle: number, center: Point2D): DrawingInterface; translate(xDist: number, yDist: number): DrawingInterface; translate(translationVector: Point2D): DrawingInterface; /** * Returns the mirror image of this drawing made with a single point (in * center mode, the default, or a plane, (plane mode, with both direction and * origin of the plane). */ mirror(centerOrDirection: Point2D, origin?: Point2D, mode?: \"center\" | \"plane\"): DrawingInterface; /** * Returns the sketched version of the drawing, on a plane */ sketchOnPlane(inputPlane: Plane): SketchInterface | Sketches; sketchOnPlane(inputPlane?: PlaneName, origin?: Point | number): SketchInterface | Sketches; sketchOnPlane(inputPlane?: PlaneName | Plane, origin?: Point | number): SketchInterface | Sketches; /** * Returns the sketched version of the drawing, on a face. * * The scale mode corresponds to the way the coordinates of the drawing are * interpreted match with the face: * * - `original` uses global coordinates (1mm in the drawing is 1mm on the * face). This is the default, but currently supported only for planar * and circular faces * - `bounds` normalises the UV parameters on the face to [0,1] intervals. * - `native` uses the default UV parameters of opencascade */ sketchOnFace(face: Face, scaleMode: ScaleMode): SketchInterface | Sketches; /** * Formats the drawing as an SVG image */ toSVG(margin: number): string; /** * Returns the SVG viewbox that corresponds to this drawing */ toSVGViewBox(margin?: number): string; /** * Formats the drawing as a list of SVG paths */ toSVGPaths(): string[] | string[][]; }\ninterface ExtrusionProfile { profile?: \"s-curve\" | \"linear\"; endFactor?: number; }\ntype FaceOrEdge = Face | Edge;\ntype FilletRadius = number | [number, number];\ntype FilterFcn<Type> = { element: Type; normal: Vector | null; };\ntype FinderFunction<FinderType, ShapeType> = (finder: FinderType, shape: ShapeType) => FinderType;\ninterface GenericSketcher<ReturnType> { /** * Changes the point to start your drawing from */ movePointerTo(point: Point2D): this; /** * Draws a line from the current point to the point given in argument * * @category Line Segment */ lineTo(point: Point2D): this; /** * Draws a line at the horizontal distance xDist and the vertical distance * yDist of the current point * * @category Line Segment */ line(xDist: number, yDist: number): this; /** * Draws a vertical line of length distance from the current point * * @category Line Segment */ vLine(distance: number): this; /** * Draws an horizontal line of length distance from the current point * * @category Line Segment */ hLine(distance: number): this; /** * Draws a vertical line to the y coordinate * * @category Line Segment */ vLineTo(yPos: number): this; /** * Draws an horizontal line to the x coordinate * * @category Line Segment */ hLineTo(xPos: number): this; /** * Draws a line from the current point to the point defined in polar * coordiates, of radius r and angle theta (in degrees) from the origin * * @category Line Segment */ polarLineTo([r, theta]: [number, number]): this; /** * Draws a line from the current point to the point defined in polar * coordiates, of radius r and angle theta (in degrees) from the current * point * * @category Line Segment */ polarLine(r: number, theta: number): this; /** * Draws a line from the current point as a tangent to the previous part of * curve drawn. The distance defines how long the line will be. * * @category Line Segment */ tangentLine(distance: number): this; /** Draws an arc of circle by defining its end point and a third point * through which the arc will pass. * * @category Arc Segment */ threePointsArcTo(end: Point2D, innerPoint: Point2D): this; /** Draws an arc of circle by defining its end point and a third point * through which the arc will pass. Both poinats are defined in horizontal * (x) and vertical (y) distances from the start point. * * @category Arc Segment */ threePointsArc(xDist: number, yDist: number, viaXDist: number, viaYDist: number): this; /** Draws an arc of circle by defining its end point and the sagitta - the * maximum distance between the arc and the straight line going from start to * end point. * * @category Arc Segment */ sagittaArcTo(end: Point2D, sagitta: number): this; /** Draws an arc of circle by defining its end point and the sagitta - the * maximum distance between the arc and the straight line going from start to * end point.The end point is defined by its horizontal and vertical * distances from the start point. * * @category Arc Segment */ sagittaArc(xDist: number, yDist: number, sagitta: number): this; /** Draws a vertical arc of circle by defining its end point and the sagitta * - the maximum distance between the arc and the straight line going from * start to end point.The end point is defined by its vertical distance from * the start point. * * @category Arc Segment */ vSagittaArc(distance: number, sagitta: number): this; /** Draws an horizontal arc of circle by defining its end point and the * sagitta - the maximum distance between the arc and the straight line going * from start to end point.The end point is defined by its horizontal * distance from the start point. * * @category Arc Segment */ hSagittaArc(distance: number, sagitta: number): this; /** Draws an arc of circle by defining its end point and the bulge - the * maximum distance between the arc and the straight line going from start to * end point. * * @category Arc Segment */ bulgeArcTo(end: Point2D, bulge: number): this; /** Draws an arc of circle by defining its end point and the bulge - the * maximum distance between the arc and the straight line going from start to * end point in units of half the chord. The end point is defined by its horizontal and vertical distances * from the start point. * * @category Arc Segment */ bulgeArc(xDist: number, yDist: number, bulge: number): this; /** Draws a vertical arc of circle by defining its end point and the bulge * - the maximum distance between the arc and the straight line going from * start to end point in units of half the chord. The end point is defined by its vertical distance from * the start point. * * @category Arc Segment */ vBulgeArc(distance: number, bulge: number): this; /** Draws an horizontal arc of circle by defining its end point and the bulge * - the maximum distance between the arc and the straight line going from * start to end point in units of half the chord. The end point is defined by * its horizontal distance from the start point. * * @category Arc Segment */ hBulgeArc(distance: number, bulge: number): this; /** * Draws an arc of circle from the current point as a tangent to the previous * part of curve drawn. * * @category Arc Segment */ tangentArcTo(end: Point2D): this; /** * Draws an arc of circle from the current point as a tangent to the previous * part of curve drawn.The end point is defined by its horizontal and vertical * distances from the start point. * * @category Arc Segment */ tangentArc(xDist: number, yDist: number): this; /** * Draws an arc of ellipse by defining its end point and an ellipse. * * The shape of the ellipse is defined by both its radiuses, its angle * relative to the current coordinat system, as well as the long and sweep * flags (as defined for SVG paths) * * @category Ellipse Arc Segment */ ellipseTo(end: Point2D, horizontalRadius: number, verticalRadius: number, rotation: number, longAxis: boolean, sweep: boolean): this; /** * Draws an arc of ellipse by defining its end point and an ellipse. The end * point is defined by distances from he start point. * * The shape of the ellipse is defined by both its radiuses, its angle * relative to the current coordinat system, as well as the long and sweep * flags (as defined for SVG paths) * * @category Ellipse Arc Segment */ ellipse(xDist: number, yDist: number, horizontalRadius: number, verticalRadius: number, rotation: number, longAxis: boolean, sweep: boolean): this; /** * Draws an arc as half an ellipse, defined by the sagitta of the ellipse * (which corresponds to the radius in the axe orthogonal to the straight * line). * * The sweep flag is to be understood as defined for SVG paths. * * @category Ellipse Arc Segment */ halfEllipseTo(end: Point2D, radius: number, sweep: boolean): this; /** * Draws an arc as half an ellipse, defined by the sagitta of the ellipse * (which corresponds to the radius in the axe orthogonal to the straight * line).The end point is defined by distances from he start point. * * The sweep flag is to be understood as defined for SVG paths. * * @category Ellipse Arc Segment */ halfEllipse(xDist: number, yDist: number, radius: number, sweep: boolean): this; /** Draws a generic bezier curve to the end point, going using a set of * control points. * * This is the generic definition of a bézier curve, you might want to use * either the quadratic or cubic (most common) version, unless you know * exactly what you are aiming at. * * @category Bezier Curve */ bezierCurveTo(end: Point2D, controlPoints: Point2D | Point2D[]): this; /** Draws a quadratic bezier curve to the end point, using the single control * point. * * @category Bezier Curve */ quadraticBezierCurveTo(end: Point2D, controlPoint: Point2D): this; /** Draws a cubic bezier curve to the end point, using the start and end * control point to define its shape. This corresponds to the most commonly * used bezier curve. * * If you are struggling setting your control points, the smoothSpline might * be better for your needs. * * @category Bezier Curve */ cubicBezierCurveTo(end: Point2D, startControlPoint: Point2D, endControlPoint: Point2D): this; /** Draws a cubic bezier curve to the end point, attempting to make the line * smooth with the previous segment. * * It will base its first control point so that its tangent is the same than * the previous segment. * * The control point relative to the end is by default set to be in the * direction of the straight line between start and end. You can specifiy the * `endSkew` either as an angle (in degrees) to this direction, or as an * absolute direction in the coordinate system (a Point). * * The start- and end- factors decide on how far the control point is from * the start and end point. At a factor of 1, the distance corresponds to * a quarter of the straight line distance. * * @category Bezier Curve */ smoothSplineTo(end: Point2D, config?: SplineConfig): this; /** Draws a cubic bezier curve to the end point, attempting to make the line * smooth with the previous segment. The end point is defined by its distance * to the first point. * * It will base its first control point so that its tangent is the same than * the previous segment. You can force another tangent by defining * `startTangent`. * * You can configure the tangent of the end point by configuring the * `endTangent`, either as \"symmetric\" to reproduce the start angle, as an * angle from the X axis (in the coordinate system) or a 2d direction (still * in the coordinate system. * * The start- and end- factors decide on how far the control point is from * the start and end point. At a factor of 1, the distance corresponds to * a quarter of the straight line distance. * * @category Bezier Curve */ smoothSpline(xDist: number, yDist: number, splineConfig: SplineConfig): this; /** * Stop drawing and returns the sketch. */ done(): ReturnType; /** * Stop drawing, make sure the sketch is closed (by adding a straight line to * from the last point to the first) and returns the sketch. */ close(): ReturnType; /** * Stop drawing, make sure the sketch is closed (by mirroring the lines * between the first and last points drawn) and returns the sketch. */ closeWithMirror(): ReturnType; }\ninterface GenericSweepConfig { frenet?: boolean; auxiliarySpine?: Wire | Edge; law?: null | Law_Function; transitionMode?: \"right\" | \"transformed\" | \"round\"; withContact?: boolean; support?: TopoDS_Shape; forceProfileSpineOthogonality?: boolean; }\ntype GenericTopo = TopologyMap[TopoEntity];\ninterface LoftConfig { ruled?: boolean; startPoint?: Point; endPoint?: Point; }\ninterface MeshOptions { tolerance?: number; angularTolerance?: number; }\ninterface MeshShapeMesh { vertices: number[]; triangles: number[]; normals: number[]; vertProperties: number[]; numProp: number; }\ninterface Offset2DConfig { lineJoinType?: \"miter\" | \"bevel\" | \"round\"; }\ninterface PlaneConfig { plane?: PlaneName | Plane; origin?: Point | number; }\ntype PlaneName = \"XY\" | \"YZ\" | \"ZX\" | \"XZ\" | \"YX\" | \"ZY\" | \"front\" | \"back\" | \"left\" | \"right\" | \"top\" | \"bottom\";\ntype PlaneSide = \"positive\" | \"negative\";\ninterface PlaneSplitResult<T> { positive: T | null; negative: T | null; on: T | null; }\ntype Point = SimplePoint | Vector | [number, number] | { XYZ: () => gp_XYZ; delete: () => void; };\ntype Point2D = [number, number];\ntype ProjectionPlane = \"XY\" | \"XZ\" | \"YZ\" | \"YX\" | \"ZX\" | \"ZY\" | \"front\" | \"back\" | \"top\" | \"bottom\" | \"left\" | \"right\";\ntype RadiusConfig<R = number> = ((e: Edge) => R | null) | R | { filter: EdgeFinder; radius: R; keep?: boolean; };\ntype ScaleMode = \"original\" | \"bounds\" | \"native\";\ntype Shape2D = Blueprint | Blueprints | CompoundBlueprint | null;\ntype Shape3D = Shell | Solid | CompSolid | Compound;\ninterface Shape3DLike<ShapeT, MeshT, OtherT = ShapeT, MeshOptionsT = any> { fuse(other: ShapeT, options?: any): ShapeT; cut(other: ShapeT, options?: any): ShapeT; intersect(other: OtherT): ShapeT; translate(xDist: number, yDist: number, zDist: number): ShapeT; translate(vector: Point): ShapeT; translateX(distance: number): ShapeT; translateY(distance: number): ShapeT; translateZ(distance: number): ShapeT; rotate(angle: number, position?: Point, direction?: Direction): ShapeT; scale(scale: number, center?: Point): ShapeT; mirror(inputPlane?: Plane | PlaneName | Point, origin?: Point): ShapeT; mesh(options?: MeshOptionsT): MeshT; readonly boundingBox: BoundingBox; }\ntype ShapeConfig = { shape: AnyShape; color?: string; alpha?: number; name?: string; };\ninterface ShapeEdgeMesh { lines: number[]; edgeGroups: { start: number; count: number; edgeId: number; }[]; }\ntype ShapeExtremumFilter = <Type extends FaceOrEdge>(filter: FilterFcn<Type>) => boolean;\ninterface ShapeMesh { triangles: number[]; vertices: number[]; normals: number[]; faceGroups: { start: number; count: number; faceId: number; }[]; }\ntype SimplePoint = [number, number, number];\ntype SingleFace = Face | FaceFinder | FinderFunction<FaceFinder, AnyShape>;\ninterface SketchInterface { /** * Transforms the lines into a face. The lines should be closed. */ face(): Face; /** * Revolves the drawing on an axis (defined by its direction and an origin * (defaults to the sketch origin) */ revolve(revolutionAxis?: Point, config?: { origin?: Point; angle?: number; }): Shape3D; /** * Extrudes the sketch to a certain distance.(along the default direction * and origin of the sketch). * * You can define another extrusion direction or origin, * * It is also possible to twist extrude with an angle (in degrees), or to * give a profile to the extrusion (the endFactor will scale the face, and * the profile will define how the scale is applied (either linarly or with * a s-shape). */ extrude(extrusionDistance: number, extrusionConfig?: { extrusionDirection?: Point; extrusionProfile?: ExtrusionProfile; twistAngle?: number; origin?: Point; }): Shape3D; /** * Loft between this sketch and another sketch (or an array of them) * * You can also define a `startPoint` for the loft (that will be placed * before this sketch) and an `endPoint` after the last one. * * You can also define if you want the loft to result in a ruled surface. * * Note that all sketches will be deleted by this operation */ loftWith(otherSketches: this | this[], loftConfig: LoftConfig, returnShell?: boolean): Shape3D; }\ntype SplineConfig = SplineTangent | { endTangent?: SplineTangent; startTangent?: StartSplineTangent; startFactor?: number; endFactor?: number; };\ntype SplineTangent = StartSplineTangent | \"symmetric\";\ntype StandardPlane = \"XY\" | \"XZ\" | \"YZ\";\ntype StartSplineTangent = number | Point2D;\ninterface STLExportOptions extends MeshOptions { binary?: boolean; }\ntype SupportedUnit = \"M\" | \"CM\" | \"MM\" | \"INCH\" | \"FT\" | \"m\" | \"mm\" | \"cm\" | \"inch\" | \"ft\";\ntype TopologyKind = (typeof TOPOLOGY_KINDS)[number];\ntype UVBounds = { uMin: number; uMax: number; vMin: number; vMax: number; };";
